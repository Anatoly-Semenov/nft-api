export type Sort = 'ascend' | 'descend' | null;

export type LeaderboardDays = 1 | 7 | 30 | 360;

export enum ProfileProcessorList {
  FetchMintedAchievements = 'fetch-minted-achievements',
}

export enum ProfileQueueList {
  FetchMintedAchievements = 'fetch-minted-achievements',
}
